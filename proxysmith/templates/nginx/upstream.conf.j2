# Generated by ProxySmith — https://github.com/dexxzy/proxysmith
# Upstream: {{ service.name }} → {{ service.host }}:{{ service.port }}
upstream {{ service.upstream_name }} {
    server {{ service.host }}:{{ service.port }};
    keepalive 32;
}
